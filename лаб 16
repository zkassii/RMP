@Composable
fun LabScreen() {
    val scrollState = rememberScrollState() //
    // ... получение времени и ресурсов (authors, hoursText и т.д.) ...

    // ВНЕШНИЙ КОНТЕЙНЕР
    Column(
        modifier = Modifier
            .fillMaxSize()
            .background(colorResource(id = R.color.background)) //
            .statusBarsPadding() // ЭТО УБЕРЕТ ТЕКСТ ИЗ-ПОД ЧЕЛКИ
    ) {
        // 1. ВЕРХНЯЯ ПАНЕЛЬ (Остается на месте)
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(all = dimensionResource(id = R.dimen.padding_standard)), //
            verticalAlignment = Alignment.CenterVertically
        ) {
            Text(
                text = stringResource(id = R.string.app_name), //
                style = MaterialTheme.typography.titleLarge.copy(fontFamily = CustomFontFamily), //
                modifier = Modifier.weight(1f)
            )
            // ... твой TextField и IconButton ...
        }

        // 2. СКРОЛЛИРУЕМАЯ ЧАСТЬ
        Column(
            modifier = Modifier
                .fillMaxWidth()
                .weight(1f) // ВАЖНО: заставляет скролл работать, занимая всё место
                .verticalScroll(scrollState) //
                .padding(horizontal = dimensionResource(id = R.dimen.padding_standard)),
            verticalArrangement = Arrangement.spacedBy(24.dp),
            horizontalAlignment = Alignment.CenterHorizontally
        ) {
            // Весь остальной контент: время, иконки, кот, авторы
            Text(
                text = "$hour $hoursText $minute $minutesText",
                style = MaterialTheme.typography.headlineMedium.copy(fontFamily = CustomFontFamily),
                color = colorResource(id = R.color.primary) //
            )

            // Твой кот с отступом (чтобы не слипался с текстом)
            Image(
                painter = painterResource(id = R.drawable.cat), //
                contentDescription = null,
                contentScale = ContentScale.Crop,
                modifier = Modifier
                    .size(dimensionResource(id = R.dimen.image_size)) //
                    .padding(top = 16.dp) // Дополнительный отступ сверху от текста
                    .clip(CircleShape)
            )
            
            // ... остальной код (authors.forEach и т.д.) ...
        }
    }
}
